To execute the linuxwork.c file, log into your linux (virtual) machine and enter the following commands:

1. gcc -o shell linuxwork.c
2. ./shell

After the commands are entered, you can input a line into the program (any sentence).

One the input is done, press "Enter" and a command response or error should pop up.

Enter 'exit' to exit the command line.
